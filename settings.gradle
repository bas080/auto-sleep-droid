pluginManagement {
    repositories {
        google()
        maven { url "https://repository.jboss.org/nexus/content/repositories/central" }
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        maven { url "https://repository.jboss.org/nexus/content/repositories/central" }
        mavenCentral()
    }
}

rootProject.name = "auto-sleep-droid"
include ":app"
